# --- Secrets : la cle Claude ne doit JAMAIS partir sur GitHub -------------
config.txt

# --- Authentification locale (genere au 1er lancement, propre a la machine) --
data/.secret
data/auth.json

# --- Cache local des immeubles (peut contenir des donnees reelles) ----------
# La graine versionnee est data/chantiers.default.json (anonymisee).
data/chantiers.json

# --- Environnement Python -------------------------------------------------
.venv/
__pycache__/
*.pyc

# --- Sorties generees (comptes rendus) et photos de travail ---------------
sortie/*
!sortie/.gitkeep
photos/*
!photos/.gitkeep

# --- Modeles de transcription telecharges (lourds, regeneres a la volee) --
*.bin
models/

# --- Node / Next.js (branche vercel-nextjs) -------------------------------
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# --- Variables d'environnement (secrets) ----------------------------------
.env
.env.local
.env*.local
.vercel

# --- Aperçus / artefacts de test (générés) --------------------------------
demo/

# --- Systeme --------------------------------------------------------------
.DS_Store
Thumbs.db

# --- Fichiers temporaires Office (verrous « ~$ » créés à l'ouverture) ------
~$*
